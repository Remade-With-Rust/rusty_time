/target
# Side target dirs, so a measurement build never waits on the lock held by a
# publish or a long test run.
/target-*
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
/corpus/results/*.json
!corpus/results/.gitkeep
# wasm-pack build output
/crates/rusty_time-wasm/pkg
